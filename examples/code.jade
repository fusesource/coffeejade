- title = "Things"
h1= title
ul#users
  -# for name, user of users
  - for name,user of users
    - if (user.isA == "ferret")
      li(class='user-' + name) #{name} is just a ferret
    - else
      li(class='user-' + name) #{name} #{user.email}